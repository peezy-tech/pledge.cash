/// <reference types="node" />

// If you also need to use it in your client-side code and it's not automatically polyfilled by Vite for all contexts:
// declare global {
//   interface Window {
//     Buffer: typeof Buffer;
//   }
// } 