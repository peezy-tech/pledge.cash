{
  "name": "@pledge.cash/web",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/deployments && cp index.html styles.css script.js favicon.svg CNAME .nojekyll dist/ && if [ -f ../../packages/contracts/deployments/998.json ]; then cp ../../packages/contracts/deployments/998.json dist/deployments/998.json; else printf '%s\\n' '{\"status\":\"pending\",\"reason\":\"Broadcast artifact not published yet\"}' > dist/deployments/998.json; fi"
  }
}
